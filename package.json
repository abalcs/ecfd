{
  "name": "ecfd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^6.5.1",
    "express": "^4.17.2"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"cd server && npm start\" \"cd client && nodemon server.js\"",
    "build": "cd client && npm run build",
    "start": "cd client && npm run start",
    "heroku-postbuild": "npm install --prefix client && npm run build --prefix client"
  },
  "author": "Alan Balcom",
  "license": "MIT"
}
